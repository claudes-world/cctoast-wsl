#!/bin/bash

# Source the kimi.sh script to make the kimi function available
source "$(dirname "$0")/.kimi/kimi.sh"

# If this script is executed directly, run the kimi function with all arguments
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    kimi "$@"
fi 